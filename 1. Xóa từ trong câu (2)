#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    getline(cin,s);
    string d;
    cin >> d;
    vector<string> v;
    stringstream ss(s);
    string h;
    while(ss>>h){
        if(h!=d){
            v.push_back(h);
        }
    }
    for(int i=0; i<v.size(); i++){
        cout << v[i];
        if(i!=v.size()-1){
        cout << " ";
    }

        
    }
    
}
