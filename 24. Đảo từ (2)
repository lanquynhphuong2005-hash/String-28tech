#include<bits/stdc++.h>
using namespace std;


int main(){
    int t; cin >> t;
    cin.ignore();
    while(t--){
        string s;
        getline(cin,s);
        stringstream ss(s);
        string tmp;
        vector<string> lock;
        while(ss>>tmp){
            lock.push_back(tmp);
        }
        for(int i=lock.size()-1; i>=0; i--){
            cout << lock[i];
            if(lock[i]!=lock[0]) cout << " ";
        }
        cout << endl;
        
       
}
}
